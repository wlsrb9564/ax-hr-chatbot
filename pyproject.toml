[project]
name = "ax-hr-chatbot"
version = "0.1.0"
description = "사내 HR 규정 Q&A 챗봇 — RAG + Tool Use Agent"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "voyageai>=0.3.0",
    "chromadb>=0.6.0",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = []
